all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ansible_playbook_python}}"
    keycloak_custom:
    apiserver:
    batchserver:
  vars:
    ansible_connection: docker